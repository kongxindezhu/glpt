<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sojson.common.dao.UDicCatalogMapper" >
  <resultMap id="BaseResultMap" type="com.sojson.common.model.UDicCatalog" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="catalogId" property="catalogId" jdbcType="VARCHAR" />
    <result column="catalogName" property="catalogName" jdbcType="VARCHAR" />
    <result column="catalogType" property="catalogType" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, catalogId,catalogName, catalogType
  </sql>
  
  <sql id="limit_sql">
  	   <if test="page_sql != null and page_sql != ''">
      	${page_sql}
      </if>
  </sql>
  
  <sql id="where_all">
  	<where>
  	<if test="findContent != null and findContent !='' " >
        and (
        LOWER(catalogId) like CONCAT("%",#{findContent,jdbcType=VARCHAR},"%") or
        LOWER(catalogName) like CONCAT("%",#{findContent,jdbcType=VARCHAR},"%")
        )
      </if>
      </where>
  </sql>
  <!-- <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.sojson.common.model.UDicCatalog" >
    insert into u_dic_catalog (id, catalogId, catalogName)
    values (#{id,jdbcType=BIGINT}, #{catalogId,jdbcType=VARCHAR}, #{catalogName,jdbcType=VARCHAR})
  </insert> -->
  
  
  <!-- 普通的分页查询 -->
  <select id="findAll" resultMap="BaseResultMap" >
  	 select 
    <include refid="Base_Column_List" />
    from u_dic_catalog 
    <include refid="where_all"/>
    <include refid="limit_sql" />
  </select>
  <select id="findCount" resultMap="BaseResultMap" >
 	 select count(id) from  u_dic_catalog
  	<include refid="where_all" />
  </select>
  

  

  

   
   
   



</mapper>
